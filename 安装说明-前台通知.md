# SullyOS 前台通知优化

这份修改包含上一次“通知已经弹出、聊天气泡却还要等一分钟”的同步修复，并增加：

- 正在 Sully 前台等待回复：不发送 iPhone 系统通知，消息直接进入聊天；
- 当前不在这个角色的聊天页：沿用 Sully 原有的应用内提示；
- Sully 退到后台：继续发送 iPhone 系统通知；
- 切换前后台或网络异常：云端收件箱保留消息，回到 Sully 后仍可补收。

## 上传文件

把压缩包里的文件按原目录覆盖到 GitHub 仓库：

- `utils/activeMsgRuntime.ts`
- `utils/amsgInstantLocalInbox.wiring.test.ts`
- `utils/amsgStateSync.ts`
- `hooks/useChatAI.ts`
- `utils/amsgInstantChat.wiring.test.ts`
- `worker/amsg/src/index.ts`
- `worker/amsg/src/instantChat.ts`
- `worker/amsg/src/instantChat.test.ts`
- `worker/amsg/worker.bundle.js`
- `public/amsg-worker.bundle.js`

## 上传后必须做

1. 等 GitHub Pages 构建完成，重新打开 SullyOS。
2. 进入“主动消息 2.0”设置，重新部署/更新一次 Cloudflare Worker。
3. iPhone 上彻底退出 SullyOS 后重新打开一次；如果仍读到旧页面，可再刷新或清一次站点缓存。

网页代码和 Worker 必须同时更新。只更新网页，系统通知仍会按旧规则弹出；只更新 Worker，前台消息可能要等旧版页面的 60 秒补收。

## 实机检查

依次测试三次：

1. 停留在萧逸聊天页发送消息：应直接出现回复，不弹系统通知。
2. 发送后切到 Sully 内其他页面：应在 Sully 内提示，不弹系统通知。
3. 发送后立即把 Sully 切到后台：回复完成时应弹 iPhone 系统通知。

极端情况下，iOS 在切后台瞬间掐断网络，可能让这一条仍多弹一次系统通知；代码选择“宁可多提醒一次，也不要漏掉后台消息或伤害推送权限”。

## 2026-08-23 修正版

第一版漏掉了即时对话发送前启动“正在前台”租约，因此即使 Worker 已更新也仍会弹系统通知。修正版已在 `hooks/useChatAI.ts` 启动并等待租约写入，收到完整回复或失败结论后才停止。请使用当前压缩包重新覆盖；然后再更新网页和 Worker。
